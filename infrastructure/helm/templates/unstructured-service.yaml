apiVersion: v1
kind: Service
metadata:  
  name: unstructured  
spec:  
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: unstructured
  sessionAffinity: None
  type: ClusterIP
